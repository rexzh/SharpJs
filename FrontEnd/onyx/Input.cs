using JavaScript;

namespace onyx
{
    /// <summary>
    /// An onyx styled Input control. In addition to the features of enyo.Input, the defaultFocus property can be set to true to focus the input when it's rendered. Only one input should be set as the defaultFocus.
    /// Typically an Input is surrounded with an onyx.InputDecorator which provides styling.
    /// </summary>
    public class Input : enyo.Input
    {
    }
}
