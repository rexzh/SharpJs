using JavaScript;

using enyo;

namespace onyx
{
    /// <summary>
    /// onyx.PickerButton is a button that, when tapped, shows an onyx.Picker. Once an item is selected, the list of items closes, but the item stays selected and the PickerButton displays the choice that was made.
    /// </summary>
    public class PickerButton : onyx.Button
    {
    }
}
