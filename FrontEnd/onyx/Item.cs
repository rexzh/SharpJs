using JavaScript;

namespace onyx
{
    /// <summary>
    /// A control designed to display a group of stacked items, typically used in lists. Items are displayed with small guide lines between them; by default, they are highlighted when tapped. Set tapHighlight to false to prevent the highlighting.
    /// </summary>
    public class Item : enyo.Control
    {
        public bool TapHighlight;
    }
}
