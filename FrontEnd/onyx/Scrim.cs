using JavaScript;

namespace onyx
{
    public class Scrim : enyo.Control
    {
        /// <summary>
        /// Hide Scrim at the specified ZIndex
        /// </summary>
        /// <param name="zIndex"></param>
        public void HideAtZIndex(int zIndex)
        {
        }

        /// <summary>
        /// Show Scrim at the specified ZIndex. Note: If you use showAtZIndex you must call hideAtZIndex to properly unwind the zIndex stack
        /// </summary>
        /// <param name="zIndex"></param>
        public void ShowAtZIndex(int zIndex)
        {
        }
    }
}
