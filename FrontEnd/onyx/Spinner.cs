using JavaScript;

namespace onyx
{
    /// <summary>
    /// A control that displays a spinner animation to indicate that activity is taking place. By default, onyx.Spinner will display a light spinner, suitable for displaying against a dark background.
    /// To render a dark spinner to be shown on a lighter background, add the "onyx-light" class to the spinner.
    /// Typically, a spinner is shown to indicate activity and hidden to indicate that the activity has ended. The spinner animation will automatically start when a spinner is shown. 
    /// If you wish, you may control the animation directly by calling the start, stop, and toggle methods.
    /// </summary>
    public class Spinner : enyo.Control
    {
        /// <summary>
        /// Starts the spinner animation.
        /// </summary>
        public void Start()
        {
        }

        /// <summary>
        /// Stops the spinner animation.
        /// </summary>
        public void Stop()
        {
        }

        /// <summary>
        /// Toggles the spinner animation on or off.
        /// </summary>
        public void Toggle()
        {
        }
    }
}
