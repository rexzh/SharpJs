namespace onyx
{
    /// <summary>
    /// An onyx styled TextArea control. In addition to the features of enyo.TextArea, the defaultFocus property can be set to true to focus the richtext when it's rendered. Only one richtext should be set as the defaultFocus.
    /// Typically an TextArea is surrounded with an onyx.InputDecorator which provides styling.
    /// </summary>
    public class TextArea : enyo.TextArea
    {
    }
}
