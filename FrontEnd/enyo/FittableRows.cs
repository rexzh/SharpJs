using System;

namespace enyo
{
    public class FittableRows : Control
    {
        /// <summary>
        /// By default items in rows stretch to fit vertically; set to false to avoid this.
        /// </summary>
        public new bool NoStretch;
    }
}
