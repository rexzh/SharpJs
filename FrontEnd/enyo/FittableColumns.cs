using System;

namespace enyo
{
    public class FittableColumns : Control
    {
        /// <summary>
        /// By default items in columns stretch to fit vertically; set to false to avoid this.
        /// </summary>
        public new bool NoStretch;
    }
}
