using System;

namespace enyo
{
    /// <summary>
    /// enyo.TouchScrollStrategy is a helper kind implementing a touch-based scroller that integrates the scrolling simulation provided by enyo.ScrollMath into an enyo.Scroller.
    /// enyo.TouchScrollStrategy is not typically created in application code.
    /// </summary>
    public class TouchScrollStrategy : ScrollStrategy
    {
        protected TouchScrollStrategy() { }
    }
}
