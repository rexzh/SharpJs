using System;

namespace enyo
{
    /// <summary>
    /// Implements an HTML button, with support for grouping.
    /// </summary>
    public class Button : ToolDecorator
    {
        public bool Disabled { get; set; }
    }
}
