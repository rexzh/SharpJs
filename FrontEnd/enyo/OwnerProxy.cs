using System;

namespace enyo
{
    public class OwnerProxy : Control
    {
        //Extend:Not expose
        //decorateEvent: function(inEventName, inEvent, inSender)
        //delegateEvent: function(inDelegate, inName, inEventName, inEvent, inSender)
    }
}
